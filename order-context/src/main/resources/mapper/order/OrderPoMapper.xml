<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xianyu.order.context.order.infr.persistence.mapper.OrderPoMapper">

    <select id="getForUpdateById" resultType="com.xianyu.order.context.order.infr.persistence.po.OrderPo">
        SELECT * FROM "order"
        WHERE order_id = #{id}
        FOR UPDATE
    </select>

</mapper>
